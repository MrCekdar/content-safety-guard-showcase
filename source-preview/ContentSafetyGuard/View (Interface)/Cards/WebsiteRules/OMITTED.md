# Omitted Website Rules UI

The complete website category management UI is intentionally omitted because it belongs to the internal product logic.

The showcase still includes other WPF cards and application shell code to demonstrate UI structure and coding style.
